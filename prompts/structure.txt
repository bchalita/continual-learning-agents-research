Analyze the provided document pages and identify:
1. Document-level metadata (shared across all sections)
2. The distinct sections the document contains

Return ONLY valid JSON — no markdown, no explanation, no code fences:
{
  "metadata": {
    "ro_number": "repair order number visible on the document",
    "vin": "vehicle identification number (17 characters)",
    "customer_name": "customer's full name",
    "dealership_name": "dealership or service center name",
    "vehicle": "year make model (e.g. 2019 BMW X7)",
    "open_date": "date the repair order was opened"
  },
  "sections": [
    {
      "prefix": "SECTION_CODE",
      "pages": [0, 1],
      "description": "Brief description of what this section contains."
    }
  ]
}

Rules for metadata:
- Extract values exactly as shown — do not guess or infer
- Use null for any field not clearly visible
- These fields typically appear in the header area of every section

Rules for sections:
- BMW repair orders use these specific section prefix codes (use ONLY these):
    ASI = Accounting Service Invoice
    BWO = BMW Work Order
    CSI = Customer Service Invoice
    ISI = Internal Service Invoice
    JSI = Job Service Invoice
    WSI = Warranty Service Invoice
- How to identify sections:
    - Some pages have the prefix code (ASI, BWO, etc.) printed explicitly in the header
    - Invoice-type pages all say "*INVOICE*" but are distinguished by the COPY TYPE
      printed at the bottom of the page:
        "CUSTOMER COPY" → CSI (Customer Service Invoice)
        "SIGNATURE COPY" → JSI (Job Service Invoice)
        "ACCOUNTING COPY" → ASI (Accounting Service Invoice)
        "WARRANTY COPY" → WSI (Warranty Service Invoice)
        "INTERNAL COPY" → ISI (Internal Service Invoice)
    - Work order pages may say "TECHNICIAN COPY" → BWO
- Each section type appears AT MOST once per repair order — do NOT duplicate prefixes
- Check the BOTTOM of each page for the copy type label
- pages is a 0-indexed list of integer page numbers belonging to this section
- If section boundaries are unclear, return one section covering all pages
- List pages in order; do not repeat a page in more than one section
